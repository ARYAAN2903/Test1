version: 0.2

phases:
  pre_build:
    commands:
      - echo "Pre-build phase: Execute commands before the build"
      - # Add any pre-build commands if needed

  build:
    commands:
      - echo "Build phase: Execute your build commands here"
      - # Add your build commands, e.g., compiling code, running tests, etc.

  post_build:
    commands:
      - echo "Post-build phase: Execute commands after the build"
      - # Add any post-build commands if needed

artifacts:
  files:
    - '**/*'  # Include all files and directories in the build artifacts
