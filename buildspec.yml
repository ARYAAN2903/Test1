version: 0.2

phases:
  build:
    commands:
      - echo "Build phase: Installing dependencies and building the Node.js application"
      - npm install  # Install Node.js dependencies
      - npm run build  # Build the application (adjust the npm script as needed)

artifacts:
  files:
    - '**/*'  # Include all files and directories in the build artifacts
